on: push
name: Rust tests

# Make sure CI fails on all warnings
env:
  RUSTFLAGS: "-Dwarnings"

jobs:
  rust_test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run tests
        run: cargo test

